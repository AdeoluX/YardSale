const User = require('./User')
const Order = require('./Orders')
const Product = require('./Product')
const Transaction = require('./Transaction')

module.exports = {
    User, Order, Product, Transaction, 
}